# Gallery images

The UI shows a couple of decorative photos here (theme avatars). The original
images are **personal family photos and are intentionally not committed** to this
public repo.

Drop your own `*.jpg` / `*.png` files in this folder to populate the gallery — the
app picks up whatever is here. Missing images are handled gracefully.
